apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-media-config-map
  namespace: {{cookiecutter.project_slug}}-production
data:
  nginx.conf: |
    worker_processes        1;

    events {
      worker_connections  512;
    }

    http {
      include    mime.types;

      server {
          location / {
              root /app/;
          }
      }
    }

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-nginx-deployment
  namespace: {{cookiecutter.project_slug}}-production
spec:
  replicas: 1
  selector:
    matchLabels:
      component: media-nginx
  template:
    metadata:
      labels:
        component: media-nginx
    spec:
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-media-config-map
        - name: media-volume
          persistentVolumeClaim:
            claimName: media-persistent-volume-claim
      containers:
        - name: nginx-media-files
          image: nginx
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            - name: media-volume
              mountPath: /app/media
              subPath: media

---

apiVersion: v1
kind: Service
metadata:
  name: media-nginx-cluster-ip-service
  namespace: {{cookiecutter.project_slug}}-production
spec:
  type: ClusterIP
  selector:
    component: media-nginx
  ports:
    - port: 80
      targetPort: 80
