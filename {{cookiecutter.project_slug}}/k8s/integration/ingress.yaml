apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: integration-{{cookiecutter.project_slug}}-ingress-service
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: integration.{{cookiecutter.project_slug}}.com
      http:
        paths:
          - path: /
            backend:
              serviceName: integration-{{cookiecutter.project_slug}}-frontend-cluster-ip-service
              servicePort: 3000
          - path: /backendstatic/
            backend:
              serviceName: integration-{{cookiecutter.project_slug}}-static-nginx-cluster-ip-service
              servicePort: 80
          - path: /media/
            backend:
              serviceName: integration-{{cookiecutter.project_slug}}-media-nginx-cluster-ip-service
              servicePort: 80
          - path: /api/
            backend:
              serviceName: integration-{{cookiecutter.project_slug}}-backend-cluster-ip-service
              servicePort: 8000
          - path: /admin/
            backend:
              serviceName: integration-{{cookiecutter.project_slug}}-backend-cluster-ip-service
              servicePort: 8000
